"use strict";

//only 1 export default per file
export default class MathStuff{
    constructor() {
        this.PI = 3.14;
    }
}

 export function addThreeNumbers(a,b,c) {
 return a + b + c;
}

export function averageOfThreeNumbers(a,b,c) {
    return (a + b + c) / 3;
}

